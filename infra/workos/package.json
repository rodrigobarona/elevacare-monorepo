{
  "name": "@eleva/infra-workos",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "rbac:generate": "tsx --env-file=../../.env.local rbac-generate.ts",
    "widgets:generate": "tsx --env-file=../../.env.local widgets-generate.ts"
  },
  "dependencies": {
    "@workos-inc/node": "catalog:"
  },
  "devDependencies": {
    "@eleva/eslint-config": "workspace:*",
    "@eleva/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
