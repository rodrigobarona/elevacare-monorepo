/**
 * Server-safe icon exports (Phosphor SSR — no React context).
 * For client wrappers (NavIcon, ElevaIcon), use `@eleva/icons/client`.
 */
export * from "./icons"
export * from "./presets"
export { NAV_ICON_NAMES, type NavIconName } from "./nav-icon-registry"
